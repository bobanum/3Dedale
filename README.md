# 3Dedale

Vanilla JavaScript 3D maze creator with:

- Full OOP structure (`Point`, `Wall`, `Wanderer`, `Maze`, `AStarSolver`, `Renderer`)
- Random wanderer maze generation
- A* solver for 3D mazes
- Renderer output with doors and ladders
- JSON export (`maze.toJSON()` / `maze.toJSONString()`)
